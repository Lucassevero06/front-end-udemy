// Criando eventos
const Events = () => {
  return (
    <div>
        <div>
            <button onClick={() => {
                console.log("object");
            }}>Clique aqui</button>
        </div>
    </div>
  )
}

export default Events